fastapi>=0.141.1
uvicorn[standard]>=0.53.0
python-multipart>=0.0.32
pydantic>=2.13.5
aiofiles>=25.1.0
httpx>=0.28.1
# [asyncio]: el engine de la app (y tests/conftest.py) usa
# sqlalchemy.ext.asyncio, que requiere greenlet en todas las plataformas.
# Sin el extra, en un venv limpio falla el import (verde solo si greenlet
# entra de paso por otra dependencia).
sqlalchemy[asyncio]>=2.0.54
aiosqlite>=0.22.1
gguf>=0.19.0
nvidia-ml-py>=13
psutil>=7.2.2

# Extracción de adjuntos del chat (módulo M3). Todas puras Python y livianas:
# si falta alguna, ese tipo de archivo se sigue adjuntando como "no
# interpretable" en vez de romper la app.
pypdf>=6.19.0
python-docx>=1.2.0
python-pptx>=1.0.2
openpyxl>=3.1

# Tools de búsqueda y navegación web del chat (módulo M3, Bloque 2).
#
# ddgs es el proveedor de búsqueda por defecto: sin servidor, sin API key y
# sin Docker, así que entra en el bundle de Tauri como una dependencia más.
# SearXNG, Brave y Tavily son alternativas opt-in y no agregan dependencias
# (SearXNG se consulta por HTTP, las otras dos por API).
#
# readability-lxml es la alternativa que usa tools.py si trafilatura no
# está o no logra extraer nada de una página.
ddgs>=9.16.0
trafilatura>=2.2.0
readability-lxml>=0.9
