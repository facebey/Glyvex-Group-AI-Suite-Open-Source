# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Node
node_modules/
frontend/node_modules/
vite.config.js.timestamp-*.mjs

# Build
frontend/dist/
# WiX (candle con varias fuentes escribe los objs en el cwd)
*.wixobj
# Artefactos PyInstaller (sidecar Tauri, T-1): los genera build.ps1
backend/build/
backend/dist/

# Documentacion local puntual (solo en disco)
REVIEW_PLAN.md

# Distribucion (clone local del repo open source; la crea sync-oss)
distribution/oss-clone/
# Instalador MSI generado por build.ps1 -MakeInstaller (WiX)
distribution/dist/
distribution/wix/files.wxi
distribution/wix/license.rtf
distribution/wix/*.wixobj
distribution/wix/obj/

# Data / logs (no commitear datos locales)
# Instancias alternativas: environments/<nombre>.env define GLYVEX_DATA_DIR,
# y por convención esos directorios se llaman data-<algo> (data-testing, etc).
data-*/
data/config.json
data/models.json
data/models_schema_version.json
data/attachments/
data/logs/
data/benchmarks/
data/runtime/
data/*.db
data/*.db-shm
data/*.db-wal
backend-prod.log

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Documentacion local (solo en disco, no versionar)
AGENTS.md